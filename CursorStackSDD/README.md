# CursorStackSDD - 协议栈 + SDD 融合规则库

> **版本**: v1.0  
> **适用**: Cursor IDE 2.4+  
> **协议**: MIT

一个可以一键部署的 **协议栈（L0-L3）+ SDD（规格驱动开发）** 融合框架。

---

## 快速开始

### 方式一：PowerShell 脚本部署（推荐）

```powershell
# 1. 把 CursorStackSDD 文件夹复制到你的项目根目录
# 2. 在项目根目录运行:
.\CursorStackSDD\deploy.ps1

# 3. (可选) 部署完成后可删除 CursorStackSDD 文件夹
```

> ⚠️ 脚本会自动排除项目相关的规则文件（UE5/TXUE/GameKit），只部署通用框架。

**或指定其他目标路径**：
```powershell
.\CursorStackSDD\deploy.ps1 -TargetPath "D:\other-project"
```

### 方式二：手动复制

```powershell
# 1. 复制 .cursor/ 目录到你的项目
Copy-Item -Recurse CursorStackSDD\.cursor\ .\

# 2. 复制 .sdd/ 目录
Copy-Item -Recurse CursorStackSDD\.sdd\ .\

# 3. 复制 specs/ 目录
Copy-Item -Recurse CursorStackSDD\specs\ .\

# 4. 复制 badcase.md
Copy-Item CursorStackSDD\badcase.md .\

# 5. 手动删除项目相关的规则文件（见下方列表）
```

---

## 文件分类说明

### ✅ 通用文件（部署时保留）

| 目录/文件 | 说明 |
|----------|------|
| `.cursor/commands/` | SDD 斜杠命令（全部保留） |
| `.cursor/agents/` | 子代理（全部保留） |
| `.cursor/skills/` | 技能包（全部保留） |
| `.cursor/hooks/` | 钩子（全部保留） |
| `.cursor/rules/sdd-system.mdc` | SDD 核心规则 |
| `.cursor/rules/INDEX.mdc` | 规则索引 |
| `.cursor/rules/L0-meta-*.mdc` | L0 元规则（通用） |
| `.cursor/rules/L3-badcase-template.mdc` | Bad Case 模板（通用） |
| `.sdd/` | SDD 配置和模板（全部保留） |
| `specs/` | 功能规格目录（全部保留） |
| `badcase.md` | Bad Case 记录 |

### ❌ 项目相关文件（部署时排除）

以下文件是 TXUE（天下 UE 基础插件）项目的专属规则，部署时会自动排除：

| 文件 | 说明 |
|------|------|
| `.cursor/rules/L1-ue5-*.mdc` | UE5 引擎规范 |
| `.cursor/rules/L2-ue5-*.mdc` | UE5 技术约束 |
| `.cursor/rules/L2-gamekit-*.mdc` | GameKit 框架约束 |
| `.cursor/rules/TXUE-rules.mdc` | TXUE 项目专属规则 |
| `specs/active/feat-001-sdd-commands/` | TXUE 示例功能规格 |

> 💡 这些文件作为**参考示例**保留在库中，你可以参考它们来编写自己项目的 L1/L2 规则。

---

## 目录结构

```
your-project/                   # 部署后的目录结构
├── .cursor/
│   ├── commands/               # SDD 斜杠命令
│   ├── agents/                 # 子代理（6 个）
│   ├── skills/                 # 技能包（5 个）
│   ├── hooks/                  # 钩子
│   └── rules/                  # 全局规则（已排除项目相关）
│       ├── sdd-system.mdc      # SDD 核心规则
│       ├── INDEX.mdc           # 规则索引
│       ├── L0-meta-*.mdc       # L0 元规则
│       └── L3-badcase-template.mdc
│
├── .sdd/                       # SDD 配置和模板
│   ├── config.json
│   ├── guidelines.md
│   └── templates/
│
├── specs/                      # SDD 功能规格
│   ├── 00-overview.md
│   ├── index.md
│   ├── active/                 # 开发中功能
│   ├── completed/              # 已完成功能
│   └── backlog/                # 待办功能
│
└── badcase.md                  # Bad Case 记录
```

---

## 核心概念

### 协议栈分层（L0-L3）

| 层级 | 名称 | 职责 | 落地工具 |
|------|------|------|---------|
| **L0** | 进化层 | 规则怎么更新 | `/evolve` + `badcase.md` |
| **L1** | 交互层 | 需求怎么传达 | `/specify`、`/plan` |
| **L2** | 基线层 | 红线不能碰 | `sdd-verifier`、`/audit` |
| **L3** | 作业层 | 出问题怎么修 | `/implement`、`sdd-orchestrator` |

### SDD 命令

| 命令 | 用途 | 产出 |
|------|------|------|
| `/brief` | 快速规划（30分钟） | `feature-brief.md` |
| `/specify` | 详细需求规格 | `spec.md` |
| `/plan` | 技术架构设计 | `plan.md` |
| `/tasks` | 任务拆解 | `tasks.md` |
| `/implement` | 执行实现 | 代码 |
| `/evolve` | 更新规格 | 触发 PDCA |
| `/audit` | 审计一致性 | 验证报告 |

---

## 工作流

### 简单功能（< 1 天）

```
/brief → /plan → /tasks → /implement
```

### 复杂功能（> 1 周）

```
/brief → /specify → /plan → /tasks → /implement → /audit
```

### 发现 AI 犯错时

```
记录 badcase.md → /evolve → 更新 .cursor/rules/ 中的规则
```

---

## 自定义配置

### 添加你的 L1/L2 规则

参考 `.cursor/rules/` 中的 UE5/GameKit 规则文件格式，创建你自己项目的规则：

**示例：Web 项目安全规则**
```markdown
# .cursor/rules/L2-web-security.mdc
---
alwaysApply: true
---

## [Constraint] SQL 注入防护
- ❌ 禁止直接拼接 SQL 字符串
- ✅ 使用参数化查询或 ORM

## [Constraint] XSS 防护
- ❌ 禁止直接输出用户输入到 HTML
- ✅ 使用模板引擎的自动转义功能
```

**示例：Python 项目规范**
```markdown
# .cursor/rules/L1-python-standards.mdc
---
alwaysApply: true
---

## [Suggestion] 类型注解
- ✅ 所有函数参数和返回值添加类型注解
- ✅ 使用 `typing` 模块的泛型类型
```

---

## 子代理说明

| 子代理 | 职责 | 对应协议栈 |
|--------|------|-----------|
| `sdd-explorer` | 探索代码库 | L1 交互层 |
| `sdd-planner` | 设计方案 | L1 交互层 |
| `sdd-implementer` | 生成代码 | L3 作业层 |
| `sdd-verifier` | 检查红线 | L2 基线层 |
| `sdd-reviewer` | 安全审查 | L2 基线层 |
| `sdd-orchestrator` | 并行协调 | L3 作业层 |

---

## 参考资源

- [spec-kit-command-cursor](https://github.com/madebyaris/spec-kit-command-cursor) - SDD 原始框架
- 认知飞轮系列文章 - 理论背景

---

## 版本记录

| 版本 | 日期 | 变更 |
|------|------|------|
| 1.0 | 2026-02-06 | 初始版本，协议栈 + SDD 融合 |
